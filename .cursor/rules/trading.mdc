---
description: 交易记录与风控规则
globs: "journal/**/*,strategies/**/*"
---

# 交易记录规则

## 币种规范

所有金额统一以**人民币 (CNY)** 为基准币种：
- `portfolio.yaml`、`trades/*.yaml`、`watchlist.yaml` 中的成本价、止损价、止盈价等字段一律使用人民币
- 港股标的同时保留一个 `original_price_hkd` 字段记录港币原始成交价，仅作为参考
- 汇率换算：港股价格按券商实际结算汇率折算为人民币；若无精确汇率，使用截图中券商显示的人民币成本作为准确值
- `exchange_rate` 字段记录当笔交易的 HKD→CNY 汇率（如 0.8767 表示 1 HKD = 0.8767 CNY）
- 讨论、报告、decision.md 中提及港股价格时标注 `HK$` 前缀以区分，但所有结构化数据字段均为人民币

## 记录规范

每笔交易必须关联：
- `research_ref`：指向触发此交易的调研目录
- `decision_ref`：指向对应的 decision.md
- `review_date`：预设的下次复盘日期

## 交易纪律

- 单只股票仓位不超过总资金 20%
- 必须设置止损价，不允许无止损的持仓
- 亏损超过 8% 时触发强制复盘
- 重大事件（财报、年报）前后 3 天内不做大仓位变动

## 复盘要求

复盘时必须回答：
1. 当时的投资论点是什么？
2. 实际发生了什么？与预期是否一致？
3. 论点是否仍然有效？
4. 学到了什么？下次怎么改进？

## 文件格式

- `journal/trades/` — YAML 格式的交易记录
- `journal/entries/` — Markdown 叙事日志
- `journal/reviews/` — Markdown 复盘
- `journal/portfolio.yaml` — 当前持仓快照
- `journal/watchlist.yaml` — 自选股与监控条件
